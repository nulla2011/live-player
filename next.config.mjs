/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack: (config, { isServer }) => {
  //   if (!isServer) {
  //     config.externals = {
  //       'flv.js': 'flvjs',
  //       'hls.js': 'Hls',
  //       artplayer: 'Artplayer',
  //     };
  //   }
  //   return config;
  // },
};

export default nextConfig;
